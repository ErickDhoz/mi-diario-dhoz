import { createTheme } from "@mui/material";

export const colorTheme = createTheme({
  palette: {
    primary: {
      main: "#6787bd",
    },
    secondary: {
      main: "#543884",
    },
    error: {
      main: "#FF0000",
    },
    circular:{
      main:'#FFFFFF'
    }
  },
});
